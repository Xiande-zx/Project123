<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectofinal">

   <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddServicio"></activity>
        <activity android:name=".EmpEmpDetalle" />
        <activity android:name=".EmpServicioDetalle" />
        <activity android:name=".EmpMenu" />
        <activity android:name=".EmpLogin" />
        <activity android:name=".ServicioListado" />
        <activity android:name=".EmpresaDetalle" />
        <activity android:name=".ServicioDetalle" />
        <activity android:name=".EmpresaListado" />
        <activity android:name=".UsuarioMenu" />
        <activity android:name=".UsuarioDetalle" android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar" />
        <activity android:name=".UsuarioRegistrar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>